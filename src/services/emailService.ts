/**
 * Email Service for DevTracker
 *
 * Note: User invitations are now handled via Firebase Magic Links.
 * This file is kept for potential future email needs (e.g., notifications).
 *
 * Firebase Magic Link authentication is configured in:
 * - src/config/firebase.ts (actionCodeSettings)
 * - src/services/userService.ts (sendSignInLinkToEmail)
 * - src/components/CompleteSignup.tsx (handles the callback)
 */

// Placeholder for future email functionality if needed
export {};
